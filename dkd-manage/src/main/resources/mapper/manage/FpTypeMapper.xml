<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dkd.manage.mapper.FpTypeMapper">
    
    <resultMap type="FpType" id="FpTypeResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="model"    column="model"    />
        <result property="image"    column="image"    />
    </resultMap>

    <sql id="selectFpTypeVo">
        select id, name, model, image from tb_fp_type
    </sql>

    <select id="selectFpTypeList" parameterType="FpType" resultMap="FpTypeResult">
        <include refid="selectFpTypeVo"/>
        <where>  
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="model != null  and model != ''"> and model = #{model}</if>
        </where>
    </select>
    
    <select id="selectFpTypeById" parameterType="Long" resultMap="FpTypeResult">
        <include refid="selectFpTypeVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertFpType" parameterType="FpType" useGeneratedKeys="true" keyProperty="id">
        insert into tb_fp_type
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">name,</if>
            <if test="model != null and model != ''">model,</if>
            <if test="image != null and image != ''">image,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">#{name},</if>
            <if test="model != null and model != ''">#{model},</if>
            <if test="image != null and image != ''">#{image},</if>
         </trim>
    </insert>

    <update id="updateFpType" parameterType="FpType">
        update tb_fp_type
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="model != null and model != ''">model = #{model},</if>
            <if test="image != null and image != ''">image = #{image},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteFpTypeById" parameterType="Long">
        delete from tb_fp_type where id = #{id}
    </delete>

    <delete id="deleteFpTypeByIds" parameterType="String">
        delete from tb_fp_type where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>